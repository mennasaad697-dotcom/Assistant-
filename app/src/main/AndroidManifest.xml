<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teacherassistant">

    <!-- الأذونات المطلوبة للتطبيق -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Teacher Assistant"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- شاشة تسجيل الدخول كبداية للتطبيق -->
        <activity
            android:name=".ui.login.LoginScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- الشاشة الرئيسية -->
        <activity
            android:name=".ui.home.HomeScreen"
            android:exported="false" />

        <!-- شاشة الطلاب -->
        <activity
            android:name=".ui.students.StudentsScreen"
            android:exported="false" />

        <!-- شاشة الحضور والغياب -->
        <activity
            android:name=".ui.attendance.AttendanceScreen"
            android:exported="false" />

        <!-- شاشة المدفوعات -->
        <activity
            android:name=".ui.payments.PaymentsScreen"
            android:exported="false" />
        <activity
            android:name=".ui.reports.ReportsScreen"
            android:exported="false" />
</application>

</manifest>
